# Reference only; do not install into the existing tf_gpu environment automatically.
torch>=2.2
transformers>=4.47
peft>=0.14
accelerate>=1.2
safetensors>=0.4
numpy>=1.24
matplotlib>=3.7
datasets>=3.0
